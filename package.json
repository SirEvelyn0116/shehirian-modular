{
  "name": "shehirian-modular",
  "version": "1.0.0",
  "description": "Modular multilingual static site for Shehirian Family",
  "workspaces": [
    "recipes-app"
  ],
  "scripts": {
    "//build": "Deliberately skips sync-google-sheets.js — fast local iteration, and avoids silently overwriting local ui-strings.json with whatever's currently in the live Sheet (which may be mid-edit or bad). netlify.toml's [build].command includes the sync; production must reflect the Sheet. Use build:full to test the exact production pipeline locally.",
    "build": "npm run build:recipes && node generate-index.js",
    "build:full": "npm run build:recipes && node sync-google-sheets.js && node generate-index.js",
    "build:recipes": "npm run build --workspace=recipes-app",
    "dev:recipes": "npm run dev --workspace=recipes-app",
    "//dev:auth-stub": "Local-only: full stack (real functions + real DB) with a stubbed Netlify Identity, since there's no real Identity backend to validate a JWT against locally. Never deployed — see scripts/dev-server.js and LOCAL_DEV.md. Run `npm run build` first.",
    "dev:auth-stub": "netlify dev:exec node scripts/dev-server.js",
    "//db:clean-test-edits": "Deletes ONLY rows in `edits` whose editor_email matches the auth-stub's fake identity (scripts/stub-identity.js) — never touches edit_log (append-only by design). Safe to run repeatedly after local dev:auth-stub sessions. See LOCAL_DEV.md.",
    "db:clean-test-edits": "netlify dev:exec node db/clean-test-edits.js",
    "preview": "npx http-server dist -p 8080 -o",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:chromium": "playwright test --project=chromium",
    "test:firefox": "playwright test --project=firefox",
    "test:webkit": "playwright test --project=webkit",
    "test:report": "playwright show-report test-results/html"
  },
  "keywords": [
    "multilingual",
    "static-site",
    "modular"
  ],
  "author": "Shehirian Family",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "dotenv": "^17.4.2",
    "googleapis": "^171.4.0"
  }
}
